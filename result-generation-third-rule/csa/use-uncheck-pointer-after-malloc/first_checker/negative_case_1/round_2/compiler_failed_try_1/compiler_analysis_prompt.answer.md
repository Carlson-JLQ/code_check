I’m checking the compiler error against the checker code and the available Clang API surface, then I’ll return a compact JSON object with the repair steps and the best search terms for the missing API usage.{"repair_steps":["Replace `Expr::getExprClass()` in `isNullConstant()`; that method is not part of the public `clang::Expr` API in this Clang version. Use the analyzer-friendly null test API instead, such as `E->isNullPointerConstant(ASTContext, Expr::NPC_* )` if you have `ASTContext`, or fall back to direct AST pattern checks already present in the checker.","Because this checker is AST-only and does not have access to `ASTContext` in the current helper, simplify the null-constant test to the forms you already need for the rule: `CXXNullPtrLiteralExpr`, `IntegerLiteral` zero, and possibly `GNUNullExpr` / `CStyleNullPtrExpr` if supported by the local Clang headers. Do not rely on `getExprClass()` for classification.","Keep the rest of the rule logic unchanged: the failure is a compile-time API mismatch, not a semantic issue in the warning flow. After the API fix, rebuild to confirm the checker compiles before revisiting any path/AST coverage gaps."],"api_search_terms":["clang::Expr::isNullPointerConstant","Expr::NPC_ValueDependentIsNotNull","CXXNullPtrLiteralExpr","GNUNullExpr","IntegerLiteral zero null pointer constant","ASTContext null pointer constant","clang::Expr class API","IgnoreParenImpCasts null check"]}