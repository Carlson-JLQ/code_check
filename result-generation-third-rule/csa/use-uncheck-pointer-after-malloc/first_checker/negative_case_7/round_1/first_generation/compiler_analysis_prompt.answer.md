I’m checking the checker shape against the compile error first, then I’ll return a minimal JSON payload with the fix steps and the API terms that matter.{"repair_steps":["Change `isSimpleNullCheckCondition` to accept `const Stmt *` if it is called with `IfStmt::getCond()` as a `Stmt *`, or cast the condition to `const Expr *` before calling the helper.","Prefer the narrower fix: update the call site in the `IfStmt` handling to use `const Expr *Cond = dyn_cast_or_null<Expr>(IF->getCond());` and guard the helper call on that typed expression.","Keep the helper signature as `const Expr *` only if every caller is expression-typed; otherwise make the helper robust to `const Stmt *` and internally `dyn_cast<Expr>`.","Rebuild the checker after the type fix and verify no other AST visitors are passing `Stmt *` into expression-only helpers.","Add or adjust a small regression test that exercises an `if (p == NULL)` guard after allocation so the checker still compiles and the null-check path is preserved."],"api_search_terms":["IfStmt::getCond", "dyn_cast<Expr>", "const Stmt * to const Expr *", "checkASTCodeBody", "Checker<check::ASTCodeBody>", "BinaryOperator equality null check", "UnaryOperator logical not null check", "DeclRefExpr VarDecl", "BugReporter::emitReport", "AnalysisManager::getAnalysisDeclContext"]}