{"repair_steps":["Fix the bad call to `Expr::isNullPointerConstant`: it requires an `ASTContext &` as the first argument, but the current code passes `E->getExprLoc()`. Use the `ASTContext` available in the checker callback path, or stop calling this helper from a static function that has no context.","Propagate `ASTContext` into any helper that needs semantic null-pointer classification. In this implementation, that means changing `isNullLiteralOrMacro` and any callers that rely on `isNullPointerConstant` so they accept `ASTContext &Ctx` as a parameter.","Prefer the AST-level null checks already supported by Clang before falling back to `isNullPointerConstant`, since the checker is currently scanning syntax only. A minimal safe form is to detect `CXXNullPtrLiteralExpr` directly and use a context-aware null-constant query only when necessary.","Rebuild the checker after the signature fix and re-run the negative-case test to confirm the plugin compiles and still emits exactly one warning for a use-before-check allocation path.","If you keep the AST walk approach, add a small regression test for `NULL`, `nullptr`, and a direct `if (ptr)` guard so the null-check path is covered and the compile-time API mismatch does not come back."],"api_search_terms":["clang::Expr::isNullPointerConstant(ASTContext &, Expr::NullPointerConstantKind)","ASTContext &","clang::ento::check::ASTCodeBody","clang::ento::CheckerContext::emitReport","clang::ento::CheckerContext::addTransition","clang::AST::Expr::IgnoreParenImpCasts","clang::CXXNullPtrLiteralExpr","clang::BinaryOperator::isComparisonOp","clang::DeclRefExpr","clang::VarDecl","clang::ento::AnalysisManager::getAnalysisDeclContext"]}