{"repair_steps":["Move the `ProgramStateTrait<PointerTrackerMap>` specialization into the `clang::ento` namespace, or qualify it exactly as required by Clang Static Analyzer state traits; the current specialization is in the anonymous namespace and cannot specialize `ento`'s trait from there.","Replace the custom `PointerTrackerMap` use with a proper immutable container pattern: `llvm::ImmutableMap` does not have a `.set()` member, so update state writes to use `ProgramState::set` with the correct `data_type`, and mutate the map through the map factory or a trait type that actually supports the expected operations.","Fix all `CheckerContext::getSVal` call sites to pass an `Expr *`, not a `VarDecl *`. For variable declarations, obtain the initializer expression or derive the region through `VarRegion`/`DeclRefExpr` instead of calling `getSVal(VD)` directly.","Correct the `getFieldRegion` call to match the actual API signature: it takes `(const FieldDecl *FD, const SubRegion *SuperRegion)`, not `(VD->getType(), BR, VD)`. Use a `FieldDecl` and the base region only.","Remove the invalid `AM.getAnalysisManager()` call. `AnalysisManager` does not expose `getAnalysisManager()` here; construct `CheckerContext` using the actual constructor arguments available in this callback, or restructure the checker so it works within the callback context passed by `ASTCodeBody`.","Add the required `Profile` support if you keep `PointerTracker` in an `ImmutableMap` key/value position. Right now `PointerTracker` lacks the hashing/folding support expected by LLVM's immutable containers; simplest fix is to avoid using `PointerTracker` as a key in an immutable set/map unless you provide the necessary `Profile` method and equality semantics.","Rework the checker flow to use the `check::ASTCodeBody` callback correctly. The current implementation tries to simulate path-sensitive transitions from a body walk, but `ASTCodeBody` only provides `const Decl *`, `AnalysisManager &`, and `BugReporter &`; any path-sensitive state management must be done through supported analyzer callbacks and `CheckerContext`-based transitions.","After the API fixes, compile again and iterate on any remaining semantic issues, especially around allocation detection and null-check tracking, since the current code mixes declaration-level traversal with expression-level state tracking in a way that is unlikely to be stable in CSA."],"api_search_terms":["clang::ento::ProgramStateTrait specialization namespace ento","clang::ento::ProgramState::set ImmutableMap data_type","clang::ento::ProgramState::get trait data_type","clang::ento::CheckerContext::getSVal const Expr *","clang::ento::CheckerContext::addTransition ProgramStateRef","clang::ento::ASTCodeBody checkASTCodeBody AnalysisManager BugReporter","clang::ento::CheckerRegistry addChecker","clang::ento::MemRegionManager getFieldRegion FieldDecl SubRegion","clang::ento::DeclRefExpr VarDecl getDecl","llvm::ImmutableMap set factory profile"]}