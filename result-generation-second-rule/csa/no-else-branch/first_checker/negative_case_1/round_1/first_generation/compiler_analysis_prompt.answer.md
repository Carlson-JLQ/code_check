I’m isolating the compile breaks against the local Clang API shape, then I’ll return just the repair plan and the API terms in JSON.{"repair_steps":["Replace the nonexistent `IfStmt::getParentIf()` logic in `findTopLevelIfChain`. Clang AST `IfStmt` does not expose a parent link, so the chain must be discovered from the statement tree you already have: recurse through `getElse()` only when the `else` arm is itself an `IfStmt`, and treat that as the `else if` chain.","Change `checkStmtForMissingElse` to take `const AnalysisDeclContext *ADC` instead of `AnalysisDeclContext *`, because `AnalysisManager::getAnalysisDeclContext(D)` returns a const pointer here. Update the call sites to pass the const pointer without dropping constness.","Keep traversal scoped to actual `if` chains. The current double loop over `Body->children()` and `CompoundStmt::body()` is redundant and can revisit the same `IfStmt` multiple times. Walk the function body once, inspect direct child statements, and recurse only through nested `IfStmt`-in-`else` links to avoid duplicate reports.","Use the terminal `IfStmt` in the chain as the violation anchor only when there is at least one `else if` and the final `else` is missing. That preserves the rule intent while avoiding false positives on standalone `if` statements.","Rebuild after the const-fix and AST-traversal fix, then verify the checker still registers and the diagnostic fires only for `if`/`else if` chains with no explicit terminal `else`."],"api_search_terms":["clang::IfStmt::getElse","clang::IfStmt","isa<IfStmt>","dyn_cast<IfStmt>","clang::ento::AnalysisManager::getAnalysisDeclContext","clang::ento::AnalysisDeclContext","const AnalysisDeclContext *","clang::ento::check::ASTCodeBody","clang::ento::CheckerContext","clang::ento::BugReporter","clang::Basic::SourceLocation","clang::Stmt::children"]}