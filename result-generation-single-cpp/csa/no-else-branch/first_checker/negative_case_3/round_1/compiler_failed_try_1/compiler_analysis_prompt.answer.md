I’m checking the analyzer API shape against the compiler errors, then I’ll turn that into a compact repair plan and the most relevant search terms.{"repair_steps":["Replace the invalid `PathSensitiveBugReport` construction. The available constructor in this Clang version requires an `ExplodedNode *`, not `ASTContext &`, so create the report from an error node, typically via `CheckerContext::generateErrorNode(...)` or the equivalent path-sensitive entry point used by existing checkers.","Stop calling removed/nonexistent `PathSensitiveBugReport` mutators. `setDeclWithIssue` and `setBugLocation` are not members of this API version, so move any uniqueness/location metadata into the constructor overload that accepts `PathDiagnosticLocation` and `const Decl *`, or drop that metadata if it is not required for the checker to function.","Use the checker-context reporting flow instead of manually assembling a report from AST-only data. This checker is registered as `check::ASTCodeBody`, but the bug report APIs shown in the headers are path-sensitive; the safer fix is to emit through a `CheckerContext` callback if possible, or explicitly create an `ExplodedNode`/`CheckerContext` bridge before reporting.","Rework the reporting callback so it matches an API that can actually emit bug reports for the current analyzer version. `ASTCodeBody` only gives `AnalysisManager &` and `BugReporter &`, which is not enough for the path-sensitive constructor being used here; either switch the checker to a path-sensitive callback family or limit the report to APIs that `BugReporter` supports directly in this version.","After changing the report construction, rebuild and verify that the remaining logic still walks nested `if` / `else if` chains correctly and only reports when the final `else` is absent."],"api_search_terms":["clang::ento::PathSensitiveBugReport constructor ExplodedNode PathDiagnosticLocation const Decl","clang::ento::PathSensitiveBugReport setDeclWithIssue removed","clang::ento::PathSensitiveBugReport setBugLocation removed","clang Static Analyzer emit bug report from ASTCodeBody BugReporter","clang::ento::CheckerContext generateErrorNode emitReport","clang::ento::check::ASTCodeBody BugReporter path sensitive checker","clang::ento::CheckerContext addTransition emitReport PathSensitiveBugReport"]}