I’m isolating the API mismatch first, then I’ll translate it into concrete repair steps and a minimal search list that would have surfaced the right constructor and location helpers.{"repair_steps":["Replace `PathDiagnosticLocation::createBegin(LocStmt, BR.getSourceManager())` with the overload that matches current Clang Static Analyzer APIs. The Stmt-based `createBegin` requires a third `StackFrameOrAnalysisDeclContext` argument, so either pass the proper analysis context if you have one, or avoid building a `PathDiagnosticLocation` from a raw `Stmt` here.","Do not construct `PathSensitiveBugReport` with `(BugType, Twine, PathDiagnosticLocation)`. In this Clang version, the available constructors take an `ExplodedNode *errorNode` as the third argument, and optional unique-location arguments only after that. Create the report with the current error node and, if needed, a unique location plus declaring `Decl`.","Rework the checker callback to use the analyzer path-sensitive callback style instead of ad hoc AST traversal if you need a real `ExplodedNode` for reporting. The current `checkASTCodeBody`/manual recursion path does not provide the analysis node context that `PathSensitiveBugReport` expects.","If you want to keep this checker AST-driven, switch from `PathSensitiveBugReport` to a plain `BugReport`-style construction path that does not require path-sensitive node state, or redesign the checker to run on a path-sensitive callback such as `checkBranchCondition` or `checkPreStmt`.","After changing the report construction, keep the source range attachment only if the chosen location object is valid for that report type; otherwise attach the range through the report API that matches the constructor you use."],"api_search_terms":["clang::ento::PathSensitiveBugReport constructor ExplodedNode PathDiagnosticLocation Decl","clang::ento::PathDiagnosticLocation::createBegin const Stmt * SourceManager StackFrameOrAnalysisDeclContext","clang::ento::BugReport PathSensitiveBugReport current error node constructor","clang Static Analyzer checkASTCodeBody BugReporter ExplodedNode available context","clang::ento::CheckerContext checkBranchCondition checkPreStmt bug report location"]}