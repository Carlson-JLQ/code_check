I’m checking the checker callback shape and the bug-report constructor usage against the available Static Analyzer APIs. The failure is in two places, so I’m validating both before I summarize a repair plan.{"repair_steps":["Replace `AM.getAnalysisDeclContext()` with `AM.getAnalysisDeclContext(D)` in `checkASTCodeBody`/`reportMissingElse` paths, or stop querying it entirely if the declaration is already available from the callback argument. The current API requires a `const Decl *` and does not have a zero-arg overload.","Fix `PathSensitiveBugReport` construction to match an existing constructor. For this checker, use the 3-arg form `PathSensitiveBugReport(const BugType&, const Twine&, const ExplodedNode *)` or the 5/6-arg form if you actually need location/uniqueness data. The current call is passing `BugReporter &` in the slot where a description string is expected, which is why overload resolution fails.","Update the report emission code to build the report from a valid `ExplodedNode`/location source rather than `BugReporter`. If the checker only needs to report at the `IfStmt`, pass the node used for the diagnostic or use the constructor that takes `LocationToUnique` and `DeclToUnique` when deduplication is required.","Rebuild after the API fix and then re-run the checker-specific negative case to confirm the compile error is gone and the emitted diagnostic still targets the intended `if-else if` chain."],"api_search_terms":["clang::ento::AnalysisManager::getAnalysisDeclContext(const Decl *D)","clang::ento::PathSensitiveBugReport constructor BugType Twine ExplodedNode","clang::ento::PathSensitiveBugReport(const BugType &, const llvm::Twine &, const ExplodedNode *)","clang::ento::PathSensitiveBugReport(const BugType &, const llvm::Twine &, const llvm::Twine &, const ExplodedNode *)","clang::ento::PathSensitiveBugReport(const BugType &, const llvm::Twine &, const ExplodedNode *, PathDiagnosticLocation, const Decl *)","clang::ento::Checker<check::ASTCodeBody> checkASTCodeBody Decl AnalysisManager BugReporter"]}