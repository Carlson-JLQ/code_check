{"repair_steps":["Replace `AM.getAnalysisDeclContext()` with the correct call that passes the current declaration, e.g. `AM.getAnalysisDeclContext(D)` or, if you only need a node for reporting, stop depending on `AnalysisManager` here and use the checker context APIs instead.","Replace `BR.getContext().addTransition()` with `CheckerContext::addTransition` or `CheckerContext::generateNonFatalErrorNode`; `BugReporter::getContext()` returns `ASTContext`, which has no transition API.","Refactor the callback to use `checkASTCodeBody(const Decl *D, AnalysisManager &AM, BugReporter &BR)` only for AST traversal, and create the exploded node through a path-sensitive checker callback when you need to emit a report. The current code mixes AST-body traversal with path-sensitive state construction in a way the CSA API does not support.","If this checker is intended to be path-sensitive, move the logic to a `check::BranchCondition` or other `CheckerContext`-based callback, then use `C.generateNonFatalErrorNode(...)` or `C.addTransition(...)` to produce the report node.","If you keep the AST-body callback, emit a plain `BugReport` only when you can do so without path-sensitive transitions; otherwise the bug report creation site needs to be redesigned around `CheckerContext`."],"api_search_terms":["clang::ento::CheckerContext::addTransition","clang::ento::CheckerContext::generateNonFatalErrorNode","clang::ento::CheckerContext::emitReport","clang::ento::CheckerContext::getState","clang::ento::CheckerContext::getAnalysisManager","clang::ento::CheckerManager::runCheckersForBranchCondition","clang::ento::check::BranchCondition","clang::StaticAnalyzer Core CheckerContext path-sensitive bug report","clang::ento::AnalysisManager::getAnalysisDeclContext(const Decl *D)","clang::ento::BugReporter ASTContext no addTransition"]}