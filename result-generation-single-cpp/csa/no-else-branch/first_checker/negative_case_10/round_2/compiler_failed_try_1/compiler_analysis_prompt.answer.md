{"repair_steps":["Replace the invalid `PathSensitiveBugReport` construction with a constructor signature that exists in this Clang version. The current call passes `(BugType, message, SourceLocation)`, but `PathSensitiveBugReport` expects an `ExplodedNode *` as the third argument, optionally followed by uniqueness parameters.","Refactor the reporting path to create an error node first, then build the report from that node. The usual Static Analyzer pattern is `if (ExplodedNode *N = C.generateErrorNode(State)) { auto R = std::make_unique<PathSensitiveBugReport>(BT, Desc, N); ... }`.","Switch this checker from AST-body-only reporting to a path-sensitive callback if the intent is to emit analyzer reports tied to execution state. `checkASTCodeBody` gives you no `ExplodedNode`, so it cannot directly satisfy `PathSensitiveBugReport` creation.","If you want a pure AST checker, do not use `PathSensitiveBugReport`. Use a non-path-sensitive report type or a different reporting API that matches AST-only analysis, but keep it consistent with how `BugReporter::emitReport` expects to receive the object.","Keep the `BugType` cached as written, but ensure the report location is derived from the generated node rather than `getLocForEndOfFile(...)` unless you specifically want a file-end diagnostic. The node-based report location will make the warning attach to the actual `if` statement.","After changing the callback/reporting model, recompile and verify there is still a valid path to `BR.emitReport(std::move(R))` from the chosen callback."],"api_search_terms":["clang::ento::PathSensitiveBugReport constructor ExplodedNode BugType Twine","clang::ento::CheckerContext generateErrorNode report bug","clang::ento::BugReporter emitReport PathSensitiveBugReport","clang::ento::check::ASTCodeBody checkASTCodeBody BugReporter","clang::ento::CheckerContext addTransition generateNonFatalErrorNode","clang::ento::CheckerContext getPredecessor ExplodedNode","clang::StaticAnalyzer Core BugReporter.h PathSensitiveBugReport"]}