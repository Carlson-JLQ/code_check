{"repair_steps":["Replace the obsolete `BugReporter::createBasicReport(...)` call. In current CSA, construct a `BugReport` directly with `std::make_unique<PathSensitiveBugReport>(BT.get(), Msg, N)` or `std::make_unique<BugReport>(...)` depending on whether you need path-sensitive tracking, then pass the `std::unique_ptr` to `BR.emitReport(...)`.","Use `CheckerContext`-style reporting APIs only where they exist. For AST-body checkers, the bug report path is typically `ExplodedNode *N = C.generateErrorNode(...)` plus `C.emitReport(std::move(R))`; `BugReporter` no longer provides the old factory helper used by this code.","Fix the ownership mismatch at `emitReport`. `BR.emitReport` takes `std::unique_ptr<BugReport>`, so do not store reports in a raw pointer variable. Keep the report in a `std::unique_ptr<BugReport>` local, add ranges through `R->addRange(...)`, then `BR.emitReport(std::move(R))`.","Verify the node creation call matches the checker callback type. In an `ASTCodeBody` checker you usually need a report node from the analysis context or a null predecessor if this is purely diagnostic; if `BR.createBasicReport` was being used only to manufacture a node, switch to the modern path-sensitive pattern used by existing checkers in this repo.","Rebuild against the same LLVM/Clang revision and check for any additional API drift around `BugType` construction, `PathSensitiveBugReport`, and `generateErrorNode` versus `generateNonFatalErrorNode`."],"api_search_terms":["clang::ento::BugReporter emitReport std::unique_ptr<BugReport>","clang::ento::PathSensitiveBugReport constructor BugType StringRef ExplodedNode","clang::ento::BugReporter createBasicReport removed","clang::ento::CheckerContext generateErrorNode generateNonFatalErrorNode","clang::ento::CheckerContext emitReport std::move(std::unique_ptr<BugReport>)","clang StaticAnalyzer BugType BugReport PathSensitiveBugReport examples"]}