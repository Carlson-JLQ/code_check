{"repair_steps":["Replace `BR.getBugReporter()` with direct use of the `BugReporter &BR` parameter; `BugReporter` does not expose a nested `getBugReporter()` accessor in this API.","Use the `BugReporter`'s `SourceManager` access pattern that exists in this Clang version, or switch to obtaining the location from `FD->getASTContext().getSourceManager()` / `BR.getSourceManager()` if available in the local headers.","Keep the report construction otherwise unchanged: build the `PathSensitiveBugReport`, set the declaration and source range, then emit it.","Recompile after the API fix and, if needed, adjust any remaining reporter/location calls to match the exact Clang Static Analyzer version in this workspace."],"api_search_terms":["clang::ento::BugReporter SourceManager","clang::ento::BugReporter getSourceManager","clang static analyzer BugReporter API current SourceManager","PathSensitiveBugReport SourceManager location","clang::ento::Checker<check::ASTCodeBody> bug report source manager"]}