{"repair_steps":["Replace the invalid `BugType` construction with the API the current clang version actually exposes. `BugType` now expects a `CheckerNameRef` or `const CheckerFrontend *`, not two raw string literals. Build it from the checker frontend registration/name used by this plugin, or switch to the frontend-based constructor if that is how the checker is registered in this codebase.","Remove use of `IfStmt::getParentASTContext()` and the whole parent-walk that depends on it. `IfStmt` does not expose that API. If you need to detect an `if`/`else if` chain, do it structurally from the current `IfStmt` by following `getElse()` through nested `IfStmt`s, which this code already mostly does.","Fix `PathDiagnosticLocation::createBegin` calls to match the current overloads. The compiler shows only `createBegin(const Stmt *, const SourceManager &, StackFrameOrAnalysisDeclContext)` and the `Decl` overload. Pass the `Stmt *` plus `BR.getSourceManager()` and the required `StackFrameOrAnalysisDeclContext`, or avoid constructing `PathDiagnosticLocation` manually if the report constructor already accepts a node/location.","Stop calling `BR.getAnalyzerOptions().getDiagnosticLocation()` and `BR.getCurrentDecl()`, because those members are not present in this analyzer API. Use the `PathSensitiveBugReport` constructor that takes an `ExplodedNode *` or a `PathDiagnosticLocation`, and derive the declaration from the current AST body if you need to associate the issue with a decl.","Tighten the checker entry point to the available AST-body callback shape. Since the registration is `Checker<check::ASTCodeBody>`, keep the logic centered on `checkASTCodeBody(const Decl *, AnalysisManager &, BugReporter &)` and avoid path-sensitive helpers unless you are also moving the checker to a path-sensitive callback such as `check::BranchCondition` or `check::PreStmt` with `CheckerContext`.","After the API fixes, recompile and verify whether the checker should report once per `if` chain or once per nested `if`. The current traversal can easily double-report the same chain unless you explicitly deduplicate by only emitting on the topmost `if` in each chain."],"api_search_terms":["clang::ento::BugType CheckerNameRef constructor","clang::ento::BugType const CheckerFrontend * constructor","clang::IfStmt parent AST API getParentMapContext getParents","clang::ento::PathSensitiveBugReport constructor ExplodedNode *","clang::Analysis::PathDiagnosticLocation createBegin Stmt SourceManager StackFrameOrAnalysisDeclContext","clang::ento::BugReporter getCurrentDecl removed","clang::ento::AnalyzerOptions getDiagnosticLocation removed","clang static analyzer check ASTCodeBody report bug from Decl","clang::ento::check::BranchCondition CheckerContext if statement checker","clang::ento::CheckerContext generateErrorNode emitReport"]}