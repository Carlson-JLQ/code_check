{"repair_steps":["Replace the `std::make_unique<PathSensitiveBugReport>(...)` call with a constructor signature that matches Clang's API. The current call passes `(BugType, message, SourceLocation)`, but `PathSensitiveBugReport` expects an `ExplodedNode *` as the third argument, not a `SourceLocation`.","Obtain the current path-sensitive node from the analyzer callback context and use that as the bug report anchor. This checker should not be implemented with `checkASTCodeBody` if it needs a `PathSensitiveBugReport`; use a path-sensitive callback such as `check::BranchCondition` or another callback that provides `CheckerContext` and an `ExplodedNode *` predecessor.","If the checker is intended to stay AST-only, switch to plain `BugReport` instead of `PathSensitiveBugReport`, because AST callbacks do not provide an exploded node needed for path diagnostics.","If you keep path-sensitive reporting, emit the report with the node/location pair expected by the analyzer, and use `PathDiagnosticLocation` only where the constructor requires it, not as a substitute for the node argument.","Keep the existing `BugType` initialization, but make sure the checker registration matches the callback type you actually implement."],"api_search_terms":["clang::ento::PathSensitiveBugReport constructor ExplodedNode *","clang::ento::BugReport vs PathSensitiveBugReport","clang::ento::check::BranchCondition CheckerContext ExplodedNode","clang::ento::CheckerContext getPredecessor addTransition","clang::ento::CheckerManager runCheckersForBranchCondition","clang::ento::check::ASTCodeBody BugReporter plain BugReport","clang::ento::PathDiagnosticLocation constructor"]}